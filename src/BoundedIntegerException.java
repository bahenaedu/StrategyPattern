public class BoundedIntegerException extends BoundedInteger
{
    public BoundedIntegerException(int value, int lower, int upper, String command)
    {
        super(value, lower, upper, command);
    }
}
